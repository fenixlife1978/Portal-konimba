rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Rules for logos
    match /logos/{logoId} {
      // Allow read access to everyone for logos
      allow read: if true;
      // Allow write access only to authenticated users (admins)
      allow write: if request.auth != null;
    }
  }
}
